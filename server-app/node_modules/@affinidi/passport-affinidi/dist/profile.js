"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.profileParser = void 0;
function profileParser(user) {
    const custom = user.custom;
    const extractedData = custom.reduce((result, obj) => {
        for (const key in obj) {
            result[key] = obj[key];
        }
        return result;
    }, {});
    const profile = {
        id: user.sub,
        userId: user.sub,
        ...extractedData,
    };
    return profile;
}
exports.profileParser = profileParser;
//# sourceMappingURL=profile.js.map