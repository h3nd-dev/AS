import { Strategy } from 'openid-client';
import { ProviderOptionsType } from '.';
export default function AffinidiStrategy(options: ProviderOptionsType): Promise<{
    client: import("openid-client").BaseClient;
    sessionKey: string;
    strategy: Strategy<unknown, import("openid-client").BaseClient>;
}>;
